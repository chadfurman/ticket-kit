{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Template settings.json deposited by change-factory init (shared mode). change-factory installs NO hooks — routing lives in the /change-factory:change-factory orientation skill. Intentionally near-empty; add repo settings freely. NEVER put worktree.bgIsolation here — that is a per-user choice for the gitignored settings.local.json."
}
